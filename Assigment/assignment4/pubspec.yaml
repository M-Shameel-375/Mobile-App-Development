name: assignment4
description: "SmartTracker - Activity tracking app with location and camera"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  geolocator: ^11.0.0
  camera: ^0.10.5
  google_maps_flutter: ^2.5.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  image_picker: ^1.0.4
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true